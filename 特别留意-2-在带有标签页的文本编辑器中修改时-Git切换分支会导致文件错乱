v2600

## 特别注意 1：

- 本目录是一个本地Git仓库，一直由 Git服务程序在实时监控。
- 在 OS默认的图形文件管理中，看到的这个Git目录与其他普通目录，在没有开启显示隐藏文件夹的设置时，没有任何区别。
- 只有使用专门的工具，才能看到本目录到底处在Git仓库的哪一个分支。

## 特别注意 2：

- 本目录Git仓库所有的分支文件，存放在{当前目录}的 .git目录下。
- {当前目录}处在不同的分支时，在{当前目录}中的文件也是不一样的。
存放在{当前目录}下的文件，是由 Git服务程序根据当前所处在的分支，从.git目录下的本地Git仓库中释放到{当前目录}的。

## 特别注意 3：

- 使用在带有标签页的文本编辑器中修改文件时，禁止进行分支的切换。
- 必须关闭编辑器所有的标签页后，才能进行分支切换；以避免同名文件出现错乱。

## 特别注意 4：

- 在终端下修改本目录下的文件时，必须设置终端的分支提示符，才能编辑，以免出现分支错乱。
- 官方方法：> https://github.com/git/git/tree/master/contrib/completion  
> https://github.com/git/git/blob/master/contrib/completion/git-prompt.sh  


version: "2"

# ubuntu 22.04 最高只支持 sphinx 4.01 ？24.04 支持 sphinx 5.0+
# 有些其他依赖。同样存在这种版本的限制。

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.10"

python:
  install:
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/source/conf.py

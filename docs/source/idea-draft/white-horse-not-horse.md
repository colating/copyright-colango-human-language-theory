# 公孙龙“白马非马”思想的本质与意义


MyST-Parser（基于 markdown-it-py）负责将 Markdown转换为 HTML，它会将 > 引用内容解析为标准的 HTML `<blockquote>`标签。然而，Sphinx 的默认主题或某些自定义主题可能没有为 `<blockquote> `提供明显的视觉样式（如左侧边框或背景色），导致它看起来像普通文本。  

---

> 责将 Markdown转ddd  
> dddddd责将 Markdown转  
